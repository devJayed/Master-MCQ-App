'use client';
import Link from 'next/link';
import { useState } from 'react';
import { api } from '../../lib/api';
export default function ForgotPassword() { const [message, setMessage] = useState(''), [error, setError] = useState(''); const submit = async (e) => { e.preventDefault(); setError(''); try { const r = await api('/auth/forgot-password', { method: 'POST', body: JSON.stringify({ email: new FormData(e.currentTarget).get('email') }) }); setMessage(r.message); } catch (x) { setError(x.message); } }; return <main className="mx-auto max-w-md p-6 pt-20"><form onSubmit={submit} className="card card-body border border-base-300 bg-base-100"><h1 className="font-display text-3xl font-bold">Reset password</h1><p className="text-sm text-base-content/60">Enter your email and we&apos;ll send a short-lived reset link.</p><input required type="email" name="email" className="input input-bordered" placeholder="Email address" />{message && <p className="text-sm text-success">{message}</p>}{error && <p className="text-sm text-error">{error}</p>}<button className="btn btn-primary">Send reset link</button><Link href="/login" className="text-center text-sm text-primary">Back to login</Link></form></main>; }
